/**
 * גישה לכללי מס מהדפדפן אינה מיושמת בכוונה — הטבלאות נשארות פרטיות ב-RLS.
 * כל השאילתות נמצאות ב-backend: `backend/src/repositories/taxRulesRepository.ts`.
 * הפרונט קורא ל-`/api/tax/*` בלבד.
 */
export {};
