-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.address_price_trends (
id bigint NOT NULL DEFAULT nextval('address_price_trends_id_seq'::regclass),
address_id bigint NOT NULL UNIQUE,
city_name text,
street_name text,
house_number text,
address text NOT NULL,
rental_yield_percent numeric,
price_increase_percent numeric,
prestige_score integer,
prestige_max integer,
median_prices_by_rooms jsonb,
quarter_prices jsonb,
raw_trends_data jsonb,
scraped_at timestamp with time zone DEFAULT now(),
created_at timestamp with time zone DEFAULT now(),
updated_at timestamp with time zone DEFAULT now(),
CONSTRAINT address_price_trends_pkey PRIMARY KEY (id)
);
CREATE TABLE public.api_snapshots (
id uuid NOT NULL DEFAULT gen_random_uuid(),
source_type text NOT NULL,
source_id text NOT NULL,
snapshot jsonb NOT NULL,
fetched_at timestamp with time zone NOT NULL DEFAULT now(),
CONSTRAINT api_snapshots_pkey PRIMARY KEY (id)
);
CREATE TABLE public.app_data_snapshots (
snapshot_key text NOT NULL,
known_ids jsonb NOT NULL DEFAULT '[]'::jsonb,
updated_at timestamp with time zone NOT NULL DEFAULT now(),
CONSTRAINT app_data_snapshots_pkey PRIMARY KEY (snapshot_key)
);
CREATE TABLE public.calculation_logs (
id uuid NOT NULL DEFAULT gen_random_uuid(),
user_id uuid,
calculator_type text NOT NULL,
input_payload jsonb NOT NULL,
result_payload jsonb NOT NULL,
rule_version_id uuid,
created_at timestamp with time zone NOT NULL DEFAULT now(),
CONSTRAINT calculation_logs_pkey PRIMARY KEY (id),
CONSTRAINT calculation_logs_rule_version_id_fkey FOREIGN KEY (rule_version_id) REFERENCES public.tax_rule_versions(id)
);
CREATE TABLE public.capital_gains_rules (
id uuid NOT NULL DEFAULT gen_random_uuid(),
rule_version_id uuid NOT NULL,
asset_type text NOT NULL,
base_tax_rate numeric,
allow_purchase_tax_deduction boolean NOT NULL DEFAULT true,
allow_broker_fee_deduction boolean NOT NULL DEFAULT true,
allow_lawyer_fee_deduction boolean NOT NULL DEFAULT true,
allow_renovation_deduction boolean NOT NULL DEFAULT true,
estimator_only boolean NOT NULL DEFAULT true,
metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
CONSTRAINT capital_gains_rules_pkey PRIMARY KEY (id),
CONSTRAINT capital_gains_rules_rule_version_id_fkey FOREIGN KEY (rule_version_id) REFERENCES public.tax_rule_versions(id)
);
CREATE TABLE public.construction_progress (
id bigint NOT NULL DEFAULT nextval('construction_progress_id_seq'::regclass),
created_at timestamp with time zone NOT NULL DEFAULT now(),
MAHOZ text,
YESHUV_LAMAS text,
ATAR text,
MISPAR_MITHAM text,
SHEM_MITHAM text,
MIGRASH text,
GUSH text,
HELKA text,
MISPAR_BINYAN text,
KOMOT_BINYAN text,
YEHIDOT_BINYAN text,
SHETAH text,
SHITAT_SHIVUK text,
TAARICH_KOBEA text,
SHNAT_HOZE text,
TAARICH_SHLAV_BNIYA_5 text,
TAARICH_SHLAV_BNIYA_7 text,
TAARICH_SHLAV_BNIYA_8 text,
TAARICH_SHLAV_BNIYA_16 text,
TAARICH_SHLAV_BNIYA_18 text,
TAARICH_SHLAV_BNIYA_29 text,
TAARICH_SHLAV_BNIYA_39 text,
TAARICH_SHLAV_BNIYA_42 text,
CONSTRAINT construction_progress_pkey PRIMARY KEY (id)
);
CREATE TABLE public.contact_submissions (
id uuid NOT NULL DEFAULT gen_random_uuid(),
created_at timestamp with time zone NOT NULL DEFAULT now(),
name text NOT NULL CHECK (char_length(TRIM(BOTH FROM name)) >= 1 AND char_length(TRIM(BOTH FROM name)) <= 200),
email text NOT NULL CHECK (char_length(TRIM(BOTH FROM email)) >= 3 AND char_length(TRIM(BOTH FROM email)) <= 320),
message text NOT NULL CHECK (char_length(TRIM(BOTH FROM message)) >= 1 AND char_length(TRIM(BOTH FROM message)) <= 5000),
CONSTRAINT contact_submissions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.dangerous_buildings_active (
id bigint NOT NULL DEFAULT nextval('dangerous_buildings_active_id_seq'::regclass),
treatment_file text NOT NULL UNIQUE,
address text NOT NULL,
street_name text,
house_number text,
city_name text,
block_number text,
parcel_number text,
building_number text,
file_number integer,
order_opening_date date,
last_visit_date date,
occupancy_status text,
treatment_status text,
from_date date,
is_banana_building boolean DEFAULT false,
raw_data jsonb,
created_at timestamp with time zone DEFAULT now(),
updated_at timestamp with time zone DEFAULT now(),
scraped_at timestamp with time zone DEFAULT now(),
CONSTRAINT dangerous_buildings_active_pkey PRIMARY KEY (id)
);
CREATE TABLE public.deals (
id uuid NOT NULL DEFAULT gen_random_uuid(),
city_id integer,
city_name text NOT NULL,
serial_no integer,
address text,
area_m2 numeric,
deal_date date,
price_nis numeric,
block_parcel_subparcel text,
property_type text,
rooms numeric,
floor text,
trend text,
source_url text,
raw jsonb,
created_at timestamp with time zone NOT NULL DEFAULT now(),
CONSTRAINT deals_pkey PRIMARY KEY (id)
);
CREATE TABLE public.featured_professionals (
id uuid NOT NULL DEFAULT gen_random_uuid(),
created_at timestamp with time zone NOT NULL DEFAULT now(),
updated_at timestamp with time zone NOT NULL DEFAULT now(),
name text NOT NULL,
headline text,
description text,
city text,
phone text,
email text,
website_url text,
whatsapp text,
image_url text,
display_order integer NOT NULL DEFAULT 0,
is_published boolean NOT NULL DEFAULT false,
CONSTRAINT featured_professionals_pkey PRIMARY KEY (id)
);
CREATE TABLE public.govmap_data (
id integer NOT NULL DEFAULT nextval('govmap_data_id_seq'::regclass),
govmap_id character varying NOT NULL UNIQUE,
layer_id character varying,
feature_name text,
feature_type character varying,
city_name character varying,
region_name character varying,
district character varying,
status character varying,
program_type character varying,
renewal_type character varying,
approval_date date,
start_date date,
end_date date,
area_size numeric,
units_count integer,
developer_name character varying,
budget numeric,
ministry_project character varying,
remarks text,
location_coordinates jsonb,
additional_data jsonb,
source_url text,
updated_at timestamp with time zone DEFAULT now(),
scraped_at timestamp with time zone DEFAULT now(),
created_at timestamp with time zone DEFAULT now(),
CONSTRAINT govmap_data_pkey PRIMARY KEY (id)
);
CREATE TABLE public.govmap_deals (
id bigint NOT NULL DEFAULT nextval('govmap_deals_id_seq'::regclass),
deal_id text NOT NULL,
govmap_layer_id integer,
deal_type text,
deal_subtype text,
asset_type text,
asset_subtype text,
city_name text,
street_name text,
house_number text,
parcel_number text,
block_number text,
price_nis numeric,
rooms numeric,
area_sqm numeric,
floor integer,
deal_date date,
lng double precision,
lat double precision,
geometry USER-DEFINED,
raw_properties jsonb NOT NULL,
source_radius numeric,
source_center USER-DEFINED,
source_request_at timestamp with time zone DEFAULT now(),
created_at timestamp with time zone DEFAULT now(),
updated_at timestamp with time zone DEFAULT now(),
CONSTRAINT govmap_deals_pkey PRIMARY KEY (id)
);
CREATE TABLE public.govmap_gushim (
id integer NOT NULL DEFAULT nextval('govmap_gushim_id_seq'::regclass),
object_id integer NOT NULL UNIQUE,
gush_num integer,
gush_suffix character varying,
status_text text,
centroid jsonb,
geom text,
raw_entity jsonb,
created_at timestamp with time zone DEFAULT now(),
updated_at timestamp with time zone DEFAULT now(),
scraped_at timestamp with time zone DEFAULT now(),
CONSTRAINT govmap_gushim_pkey PRIMARY KEY (id)
);
CREATE TABLE public.govmap_gushim_by_search (
id bigint NOT NULL DEFAULT nextval('govmap_gushim_by_search_id_seq'::regclass),
govmap_object_id bigint NOT NULL UNIQUE,
layer_name text NOT NULL,
gush_num integer NOT NULL,
gush_suffix integer NOT NULL DEFAULT 0,
status_text text,
centroid_geom USER-DEFINED,
geom USER-DEFINED,
raw_entity jsonb,
scraped_at timestamp with time zone,
CONSTRAINT govmap_gushim_by_search_pkey PRIMARY KEY (id)
);
CREATE TABLE public.govmap_gushim_parcels_by_search (
id bigint NOT NULL DEFAULT nextval('govmap_gushim_parcels_by_search_id_seq'::regclass),
govmap_object_id bigint NOT NULL UNIQUE,
layer_name text NOT NULL,
gush_num integer NOT NULL,
gush_suffix integer NOT NULL DEFAULT 0,
status_text text,
parcel integer,
legal_area_m2 numeric,
note text,
centroid_geom USER-DEFINED,
geom USER-DEFINED,
raw_entity jsonb,
scraped_at timestamp with time zone,
CONSTRAINT govmap_gushim_parcels_by_search_pkey PRIMARY KEY (id)
);
CREATE TABLE public.govmap_parcels (
id integer NOT NULL DEFAULT nextval('govmap_parcels_id_seq'::regclass),
object_id integer NOT NULL UNIQUE,
gush_num integer,
gush_suffix character varying,
parcel integer,
legal_area numeric,
status_text text,
note text,
centroid jsonb,
geom text,
raw_entity jsonb,
created_at timestamp with time zone DEFAULT now(),
updated_at timestamp with time zone DEFAULT now(),
scraped_at timestamp with time zone DEFAULT now(),
CONSTRAINT govmap_parcels_pkey PRIMARY KEY (id)
);
CREATE TABLE public.govmap_parcels_data (
id bigint NOT NULL DEFAULT nextval('govmap_parcels_data_id_seq'::regclass),
objectid integer NOT NULL UNIQUE,
gush_num integer,
gush_suffix text,
parcel integer,
legal_area numeric,
status_text text,
note text,
raw_data jsonb,
created_at timestamp with time zone DEFAULT now(),
updated_at timestamp with time zone DEFAULT now(),
CONSTRAINT govmap_parcels_data_pkey PRIMARY KEY (id)
);
CREATE TABLE public.govmap_plans (
pk bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
objectid bigint NOT NULL UNIQUE,
tochnit text,
migrash text,
mishasava bigint,
kodyeud bigint,
shape_area numeric,
shape_length numeric,
coordinates jsonb,
bbox jsonb,
fetched_at timestamp with time zone DEFAULT now(),
raw jsonb NOT NULL,
CONSTRAINT govmap_plans_pkey PRIMARY KEY (pk)
);
CREATE TABLE public.govmap_plans_rami (
id integer NOT NULL DEFAULT nextval('govmap_plans_rami_id_seq'::regclass),
object_id integer NOT NULL UNIQUE,
tochnit character varying,
prjname text,
statustochnit character varying,
ishuvim text,
yazam text,
yeudikari text,
ydmelay integer,
merchavrmi integer,
prjstatus character varying,
centroid jsonb,
geom text,
raw_entity jsonb,
created_at timestamp with time zone DEFAULT now(),
updated_at timestamp with time zone DEFAULT now(),
scraped_at timestamp with time zone DEFAULT now(),
CONSTRAINT govmap_plans_rami_pkey PRIMARY KEY (id)
);
CREATE TABLE public.govmap_talar_prep (
id bigint NOT NULL DEFAULT nextval('govmap_talar_prep_id_seq'::regclass),
created_at timestamp with time zone DEFAULT now(),
object_id integer UNIQUE,
geom text,
centroid jsonb,
fields jsonb,
raw_entity jsonb,
CONSTRAINT govmap_talar_prep_pkey PRIMARY KEY (id)
);
CREATE TABLE public.govmap_urban_renewal_compounds (
object_id integer NOT NULL,
caption text,
heara text,
kishur text,
source text,
project_name text,
city_name text,
city_code integer,
neighborhood_name text,
status text,
approval_stage text,
housing_units integer,
planned_units integer,
executing_body text,
last_update date,
remarks text,
centroid USER-DEFINED,
geom USER-DEFINED,
CONSTRAINT govmap_urban_renewal_compounds_pkey PRIMARY KEY (object_id)
);
CREATE TABLE public.industrial_lots_development_areas (
id bigint NOT NULL DEFAULT nextval('industrial_lots_development_areas_id_seq'::regclass),
govmap_object_id bigint NOT NULL UNIQUE,
layer_name text NOT NULL,
lotnum text,
cdpnum text,
size_m2 numeric,
ipnameheb text,
ipactive boolean,
lotidstring text,
cdpidstring text,
disclaimerheb text,
disclaimereng text,
disclaimerarab text,
gisstatusheb text,
gisstatuseng text,
gisstatusarab text,
landdesignationassignment text,
centroid_geom USER-DEFINED,
geom USER-DEFINED,
raw_entity jsonb,
scraped_at timestamp with time zone,
CONSTRAINT industrial_lots_development_areas_pkey PRIMARY KEY (id)
);
CREATE TABLE public.karkarank_categories (
pk bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
wp_id bigint NOT NULL UNIQUE,
name text,
slug text,
parent_id bigint,
count integer,
description text,
fetched_at timestamp with time zone DEFAULT now(),
raw jsonb NOT NULL,
CONSTRAINT karkarank_categories_pkey PRIMARY KEY (pk)
);
CREATE TABLE public.karkarank_comments (
pk bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
wp_id bigint NOT NULL UNIQUE,
post_id bigint,
parent_id bigint,
author_name text,
author_email text,
author_url text,
link text,
status text,
date_gmt timestamp with time zone,
fetched_at timestamp with time zone DEFAULT now(),
content text,
raw jsonb NOT NULL,
CONSTRAINT karkarank_comments_pkey PRIMARY KEY (pk)
);
CREATE TABLE public.karkarank_posts (
pk bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
wp_id bigint NOT NULL UNIQUE,
slug text,
title text,
status text,
author_id bigint,
link text,
date_gmt timestamp with time zone,
modified_gmt timestamp with time zone,
categories ARRAY,
tags ARRAY,
comment_status text,
ping_status text,
fetched_at timestamp with time zone DEFAULT now(),
raw jsonb NOT NULL,
CONSTRAINT karkarank_posts_pkey PRIMARY KEY (pk)
);
CREATE TABLE public.land_use_mavat (
id bigint NOT NULL DEFAULT nextval('land_use_mavat_id_seq'::regclass),
govmap_object_id bigint NOT NULL UNIQUE,
layer_name text NOT NULL,
mavat_code integer,
mavat_name text,
pl_id bigint,
pl_number text,
pl_name text,
station_desc text,
last_update_date timestamp with time zone,
defq text,
source_code text,
area numeric,
len numeric,
centroid_geom USER-DEFINED,
geom USER-DEFINED,
raw_entity jsonb,
scraped_at timestamp with time zone,
CONSTRAINT land_use_mavat_pkey PRIMARY KEY (id)
);
CREATE TABLE public.meirim_plans (
id integer NOT NULL DEFAULT nextval('meirim_plans_id_seq'::regclass),
meirim_id integer NOT NULL UNIQUE,
plan_number character varying,
plan_name text,
plan_display_name text,
county_name character varying,
plan_character_name character varying,
status character varying,
goals_from_mavat text,
main_details_from_mavat text,
geometry jsonb,
object_id integer,
entity_subtype_desc character varying,
plan_url text,
mp_id bigint,
plan_new_mavat_url text,
tags jsonb,
updated_at timestamp with time zone,
scraped_at timestamp with time zone DEFAULT now(),
created_at timestamp with time zone DEFAULT now(),
CONSTRAINT meirim_plans_pkey PRIMARY KEY (id)
);
CREATE TABLE public.michrazim (
pk bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
michraz_id bigint UNIQUE,
tender_id bigint UNIQUE,
tender_number text UNIQUE,
title text,
status text,
area text,
publication_date timestamp with time zone,
deadline_date timestamp with time zone,
source_endpoint text,
fetched_at timestamp with time zone DEFAULT now(),
raw jsonb NOT NULL,
CONSTRAINT michrazim_pkey PRIMARY KEY (pk)
);
CREATE TABLE public.michrazim_active (
pk bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
michraz_id bigint NOT NULL UNIQUE,
active_since timestamp with time zone DEFAULT now(),
last_seen_at timestamp with time zone DEFAULT now(),
source_endpoint text,
raw jsonb NOT NULL,
CONSTRAINT michrazim_active_pkey PRIMARY KEY (pk)
);
CREATE TABLE public.mitchamim_rashut (
id integer NOT NULL DEFAULT nextval('mitchamim_rashut_id_seq'::regclass),
mitcham_id character varying NOT NULL UNIQUE,
mitcham_name text,
city_name character varying,
region_name character varying,
district character varying,
status character varying,
program_type character varying,
renewal_type character varying,
approval_date date,
start_date date,
end_date date,
area_size numeric,
units_count integer,
developer_name character varying,
budget numeric,
ministry_project character varying,
remarks text,
location_coordinates jsonb,
additional_data jsonb,
source_url text,
updated_at timestamp with time zone DEFAULT now(),
scraped_at timestamp with time zone DEFAULT now(),
created_at timestamp with time zone DEFAULT now(),
execution_status character varying,
plan_number character varying,
plan_url text,
map_url text,
CONSTRAINT mitchamim_rashut_pkey PRIMARY KEY (id)
);
CREATE TABLE public.msbs_lots (
object_id integer NOT NULL,
lot_id integer,
mis_toc integer,
land_use_code integer,
land_use_old_code integer,
land_use_name text,
lot_number integer,
marketing_block_id integer,
parking_spaces integer,
road_width_m numeric,
road_rights_left numeric,
road_rights_right numeric,
housing_units integer,
commerce_area_sqm numeric,
services_area_sqm numeric,
building_count integer,
building_height_m numeric,
floor_count integer,
floor_below_zero integer,
data_entry_date date,
data_source_type integer,
centroid USER-DEFINED,
geom USER-DEFINED,
raw_properties jsonb,
inserted_at timestamp with time zone DEFAULT now(),
CONSTRAINT msbs_lots_pkey PRIMARY KEY (object_id)
);
CREATE TABLE public.nadlan_jobs (
id text NOT NULL,
status text NOT NULL CHECK (status = ANY (ARRAY['queued'::text, 'running'::text, 'done'::text, 'error'::text])),
created_at timestamp with time zone DEFAULT now(),
updated_at timestamp with time zone DEFAULT now(),
result jsonb,
error text,
CONSTRAINT nadlan_jobs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.notification_preferences (
user_id uuid NOT NULL,
enabled boolean DEFAULT true,
created_at timestamp with time zone NOT NULL DEFAULT now(),
updated_at timestamp with time zone NOT NULL DEFAULT now(),
CONSTRAINT notification_preferences_pkey PRIMARY KEY (user_id),
CONSTRAINT notification_preferences_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.parcel_ownership_new (
id bigint NOT NULL DEFAULT nextval('parcel_ownership_new_id_seq'::regclass),
govmap_object_id bigint NOT NULL UNIQUE,
layer_name text NOT NULL,
gush_num integer NOT NULL,
gush_suffi integer NOT NULL,
parcel integer NOT NULL,
legal_area_m2 numeric,
ownership_type text NOT NULL,
remark text,
doc_url text,
centroid_geom USER-DEFINED,
geom USER-DEFINED,
raw_entity jsonb,
scraped_at timestamp with time zone,
CONSTRAINT parcel_ownership_new_pkey PRIMARY KEY (id)
);
CREATE TABLE public.password_reset_tokens (
id uuid NOT NULL DEFAULT gen_random_uuid(),
user_id uuid NOT NULL,
token_hash text NOT NULL,
expires_at timestamp with time zone NOT NULL,
used_at timestamp with time zone,
created_at timestamp with time zone NOT NULL DEFAULT now(),
CONSTRAINT password_reset_tokens_pkey PRIMARY KEY (id),
CONSTRAINT password_reset_tokens_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.portfolio_items (
id uuid NOT NULL DEFAULT gen_random_uuid(),
user_id uuid NOT NULL,
item_type text NOT NULL,
source_id text NOT NULL,
snapshot jsonb,
created_at timestamp with time zone NOT NULL DEFAULT now(),
CONSTRAINT portfolio_items_pkey PRIMARY KEY (id),
CONSTRAINT portfolio_items_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.purchase_tax_adjustments (
id uuid NOT NULL DEFAULT gen_random_uuid(),
rule_version_id uuid NOT NULL,
code text NOT NULL,
label_he text NOT NULL,
adjustment_type text NOT NULL,
value numeric,
metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
CONSTRAINT purchase_tax_adjustments_pkey PRIMARY KEY (id),
CONSTRAINT purchase_tax_adjustments_rule_version_id_fkey FOREIGN KEY (rule_version_id) REFERENCES public.tax_rule_versions(id)
);
CREATE TABLE public.purchase_tax_brackets (
id uuid NOT NULL DEFAULT gen_random_uuid(),
rule_version_id uuid NOT NULL,
buyer_category text NOT NULL,
min_amount numeric NOT NULL DEFAULT 0,
max_amount numeric,
rate numeric NOT NULL,
fixed_amount numeric,
sort_order integer NOT NULL DEFAULT 0,
CONSTRAINT purchase_tax_brackets_pkey PRIMARY KEY (id),
CONSTRAINT purchase_tax_brackets_rule_version_id_fkey FOREIGN KEY (rule_version_id) REFERENCES public.tax_rule_versions(id)
);
CREATE TABLE public.saved_calculations (
id uuid NOT NULL DEFAULT gen_random_uuid(),
user_id uuid NOT NULL,
title text NOT NULL,
calculator_type text NOT NULL,
input_payload jsonb NOT NULL,
result_payload jsonb NOT NULL,
created_at timestamp with time zone NOT NULL DEFAULT now(),
updated_at timestamp with time zone NOT NULL DEFAULT now(),
CONSTRAINT saved_calculations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.spatial_ref_sys (
srid integer NOT NULL CHECK (srid > 0 AND srid <= 998999),
auth_name character varying,
auth_srid integer,
srtext character varying,
proj4text character varying,
CONSTRAINT spatial_ref_sys_pkey PRIMARY KEY (srid)
);
CREATE TABLE public.taba_plans (
pk bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
plan_id bigint UNIQUE,
id bigint,
plan_number text UNIQUE,
plan_name text,
status text,
area text,
publication_date timestamp with time zone,
from_date timestamp with time zone,
to_date timestamp with time zone,
source_endpoint text,
fetched_at timestamp with time zone DEFAULT now(),
raw jsonb NOT NULL,
CONSTRAINT taba_plans_pkey PRIMARY KEY (pk)
);
CREATE TABLE public.taba_plans_all_pages (
id integer NOT NULL DEFAULT nextval('taba_plans_all_pages_id_seq'::regclass),
plan_id character varying NOT NULL,
plan_number character varying,
plan_name text,
status character varying,
area character varying,
publication_date timestamp with time zone,
source_endpoint text,
raw jsonb,
page_number integer,
created_at timestamp with time zone DEFAULT now(),
CONSTRAINT taba_plans_all_pages_pkey PRIMARY KEY (id)
);
CREATE TABLE public.taba_plans_page_1 (
id integer NOT NULL DEFAULT nextval('taba_plans_page_1_id_seq'::regclass),
plan_id character varying NOT NULL UNIQUE,
plan_number character varying,
plan_name text,
status character varying,
area character varying,
publication_date timestamp with time zone,
source_endpoint text,
raw jsonb,
page_number integer DEFAULT 1,
created_at timestamp with time zone DEFAULT now(),
CONSTRAINT taba_plans_page_1_pkey PRIMARY KEY (id)
);
CREATE TABLE public.taba_plans_page_2 (
id integer NOT NULL DEFAULT nextval('taba_plans_page_2_id_seq'::regclass),
plan_id character varying NOT NULL UNIQUE,
plan_number character varying,
plan_name text,
status character varying,
area character varying,
publication_date timestamp with time zone,
source_endpoint text,
raw jsonb,
page_number integer DEFAULT 2,
created_at timestamp with time zone DEFAULT now(),
CONSTRAINT taba_plans_page_2_pkey PRIMARY KEY (id)
);
CREATE TABLE public.taba_plans_page_3 (
id integer NOT NULL DEFAULT nextval('taba_plans_page_3_id_seq'::regclass),
plan_id character varying NOT NULL UNIQUE,
plan_number character varying,
plan_name text,
status character varying,
area character varying,
publication_date timestamp with time zone,
source_endpoint text,
raw jsonb,
page_number integer DEFAULT 3,
created_at timestamp with time zone DEFAULT now(),
CONSTRAINT taba_plans_page_3_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tama70_plans (
id bigint NOT NULL DEFAULT nextval('tama70_plans_id_seq'::regclass),
govmap_object_id bigint NOT NULL UNIQUE,
layer_name text NOT NULL,
plan_number text NOT NULL,
plan_name text NOT NULL,
last_update_raw text,
link_url text,
centroid_geom USER-DEFINED,
geom USER-DEFINED,
raw_entity jsonb,
scraped_at timestamp with time zone,
CONSTRAINT tama70_plans_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tax_rule_versions (
id uuid NOT NULL DEFAULT gen_random_uuid(),
tax_type text NOT NULL CHECK (tax_type = ANY (ARRAY['purchase'::text, 'capital_gains'::text])),
version_name text NOT NULL,
effective_from date NOT NULL,
effective_to date,
is_active boolean NOT NULL DEFAULT true,
notes text,
created_at timestamp with time zone NOT NULL DEFAULT now(),
CONSTRAINT tax_rule_versions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.telegram_documents (
id bigint NOT NULL DEFAULT nextval('telegram_documents_id_seq'::regclass),
telegram_message_id bigint,
telegram_chat_id bigint,
telegram_date timestamp with time zone,
image_file_id text,
image_url text,
raw_ocr_text text,
document_type text,
location_city text,
location_address text,
property_type text,
property_floors integer,
property_units integer,
total_area_sqm numeric,
building_area_sqm numeric,
apartments jsonb,
sale_conditions text,
submission_deadline date,
deposit_amount numeric,
deposit_currency text,
contact_name text,
contact_title text,
contact_address text,
contact_phone text,
contact_fax text,
contact_email text,
court_file_number text,
parcel_number text,
block_number text,
extracted_data jsonb,
processed_at timestamp with time zone DEFAULT now(),
processing_status text DEFAULT 'pending'::text,
error_message text,
created_at timestamp with time zone DEFAULT now(),
updated_at timestamp with time zone DEFAULT now(),
CONSTRAINT telegram_documents_pkey PRIMARY KEY (id)
);
CREATE TABLE public.urban_renewal_cities (
id integer NOT NULL DEFAULT nextval('urban_renewal_cities_id_seq'::regclass),
object_id integer UNIQUE,
city_id integer,
city_name character varying,
mitcham_count integer DEFAULT 0,
mitcham_existing_housing_units integer DEFAULT 0,
mitcham_proposed_housing_units integer DEFAULT 0,
mitcham_new_housing_units integer DEFAULT 0,
tama38_count integer DEFAULT 0,
link text,
phone_number character varying,
email character varying,
symbol_type integer,
shape_area numeric,
shape_length numeric,
created_at timestamp without time zone DEFAULT now(),
updated_at timestamp without time zone DEFAULT now(),
CONSTRAINT urban_renewal_cities_pkey PRIMARY KEY (id)
);
CREATE TABLE public.urban_renewal_locations (
id integer NOT NULL DEFAULT nextval('urban_renewal_locations_id_seq'::regclass),
object_id integer UNIQUE,
layer_id integer,
geometry_type character varying,
coordinates jsonb,
shape_area numeric,
shape_length numeric,
created_at timestamp without time zone DEFAULT now(),
updated_at timestamp without time zone DEFAULT now(),
CONSTRAINT urban_renewal_locations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.urban_renewal_mitchamim_rashut (
id bigint NOT NULL DEFAULT nextval('urban_renewal_mitchamim_rashut_id_seq'::regclass),
mispar_mitham integer NOT NULL UNIQUE,
yeshuv text,
semel_yeshuv integer,
shem_mitcham text,
yachad_kayam integer,
yachad_tosafti integer,
yachad_mutza integer,
taarich_hachraza date,
mispar_tochnit text,
kishur_latar text,
kishur_la_mapa text,
sach_heterim integer,
maslul text,
shnat_matan_tokef integer,
bebitzua text,
status text,
imported_at timestamp with time zone DEFAULT now(),
CONSTRAINT urban_renewal_mitchamim_rashut_pkey PRIMARY KEY (id)
);
CREATE TABLE public.urban_renewal_projects (
id integer NOT NULL DEFAULT nextval('urban_renewal_projects_id_seq'::regclass),
object_id integer UNIQUE,
project_number character varying,
project_name character varying,
city_name character varying,
plan_name character varying,
plan_link text,
project_type character varying,
project_subtype character varying,
status_code integer,
authority_status_code integer,
valid_date timestamp without time zone,
existing_units integer,
proposed_units integer,
additional_units integer,
notes text,
shape_area numeric,
shape_length numeric,
created_at timestamp without time zone DEFAULT now(),
updated_at timestamp without time zone DEFAULT now(),
CONSTRAINT urban_renewal_projects_pkey PRIMARY KEY (id)
);
CREATE TABLE public.user_notification_preferences (
user_id uuid NOT NULL,
notify_urban_renewal_new boolean NOT NULL DEFAULT false,
updated_at timestamp with time zone NOT NULL DEFAULT now(),
notify_dangerous_buildings_new boolean NOT NULL DEFAULT false,
CONSTRAINT user_notification_preferences_pkey PRIMARY KEY (user_id),
CONSTRAINT user_notification_preferences_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.user_notifications (
id uuid NOT NULL DEFAULT gen_random_uuid(),
user_id uuid NOT NULL,
type text NOT NULL,
title text NOT NULL,
body text,
link text,
read_at timestamp with time zone,
metadata jsonb,
created_at timestamp with time zone NOT NULL DEFAULT now(),
CONSTRAINT user_notifications_pkey PRIMARY KEY (id),
CONSTRAINT user_notifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.users (
id uuid NOT NULL DEFAULT gen_random_uuid(),
email text NOT NULL UNIQUE,
password_hash text NOT NULL,
role text NOT NULL DEFAULT 'user'::text CHECK (role = ANY (ARRAY['user'::text, 'admin'::text])),
created_at timestamp with time zone NOT NULL DEFAULT now(),
updated_at timestamp with time zone NOT NULL DEFAULT now(),
CONSTRAINT users_pkey PRIMARY KEY (id)
);
CREATE TABLE public.xplan_features (
id bigint NOT NULL DEFAULT nextval('xplan_features_id_seq'::regclass),
layer_id integer NOT NULL,
feature_objectid bigint,
plan_id text,
plan_name text,
plan_type text,
plan_status text,
plan_stage text,
district text,
municipality text,
area_dunam numeric,
submission_date date,
approval_date date,
data_source text DEFAULT 'xplan_without_77_78'::text,
geometry USER-DEFINED NOT NULL,
raw_attributes jsonb NOT NULL,
source_bbox USER-DEFINED,
inserted_at timestamp with time zone DEFAULT now(),
updated_at timestamp with time zone DEFAULT now(),
CONSTRAINT xplan_features_pkey PRIMARY KEY (id)
);